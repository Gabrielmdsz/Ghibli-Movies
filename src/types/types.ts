export interface Film {
    id: number
    title: string
    image: string
}

export interface Filmdetails {
    id: number
    title: string
    image: string
    description: string
    director: string
    producer: string
    release_date: number
    rt_score: number
}